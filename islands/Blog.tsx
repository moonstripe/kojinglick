/** @jsx h */
import { h } from "preact";
import { tw } from "@twind";
import { Handlers, PageProps } from "$fresh/server.ts";

export default (prop: PageProps) => {

    return (
        <h1>hello</h1>
    )
}